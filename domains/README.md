# Domains

This directory separates reviewed demo domains from candidate domains.

- `validated/`: rule packages used by the current local benchmark suite.
- `candidate/`: experimental rule packages that have not been manually reviewed to the same level.

These packages are for research and testing. They are not legal advice and should not be used as professional rules without independent review.
