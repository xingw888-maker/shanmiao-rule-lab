# T5.4 执行简报

> 发往新线程。包含执行 T5.4 所需全部上下文。
> 说明：这是历史执行简报，部分指标是执行前目标。最终公开指标以 `README.md`、`docs/T5_TECH_NOTE.md` 和 `tests/road2_compare_report.json` 为准：LLM 提取准确率 93.48%，REGRESSION 1，稳定剩余错误 2。

## 你是谁

你接替了 "CTO succession risk analysis" 线程（session ID: `local_3f380420-efc7-4f16-a7d9-0070f1384de4`）的工作。这个线程标题有误导——实际做的是**山苗 Kernel 路二验证**，已连续推进 T5.1→T5.2→T5.3-B→T5.3-C 四个阶段。

## 当前状态总览

```
准确率演进：
  58.7% (T5.1基线) → 76.09% (分发修复) → 89.13% (LLM提取) → 93.48% (clause模式)
  
正则: 78.26% (36/46)
LLM提取: 93.48% (43/46)
REGRESSION: 0
pytest: 76/76 passed

剩余3个不可修项:
  cn-003-POS-02 "合理使用年限" → 法律概念，非数值表达式
  cn-008-FP-01 "28天提交结算≠验收期限" → 本次要修
  cn-010-POS-01 多阶段付款求和 → 本次要修/改善
```

## 核心架构（务必理解）

```
合同文本 → [clause_splitter: 切成带类型标签的block] → [handler: 逐规则判定]
                                                    ↑
                                          LLM提取器: 结构化数值注入
                                          (仅numeric_comparison/sum_numeric)
```

**宪法约束**：handler 代码冻结（`app/engine/handlers/numeric_comparison.py` 不可改）。core.py 不可改。LLM 只做数值提取不做合规判定。

**关键机制**：`numeric_comparison.py:85-97` 已经有 clause_type 过滤逻辑——如果规则有 `clause_type` 字段且 clause_blocks 有对应类型标签，handler 只搜匹配的 block。**这段代码已经在，只是没有规则触发它。**

## T5.4 目标

修 cn-008-FP-01 和 cn-010-POS-01。两者根因相同：handler 在全文搜数字，不知道"28天"来自结算条款还是验收条款。

**核心洞察**：clause_splitter 输出的 `auto_label` 是 DBSCAN 动态生成的（如"数字密集-保修防水年限-后段"），随合同变化。rules.json 需要固定类型名。必须建一个中间层做标准化。

## 你的任务（两步）

### 第一步：补 clause_splitter._infer_clause_type 关键词表

`app/engine/clause_splitter.py` 第 166-172 行当前是空壳：

```python
def _infer_clause_type(clause: ClauseBlock) -> tuple[str, float]:
    return _DEFAULT_TYPE, 0.0
```

**不用建新文件。** 直接把这个函数填成关键词匹配，让它输出固定类型名。

**关键词→类型映射表**（匹配 clause_title + content 前 200 字）：

```python
_CLAUSE_TYPE_KEYWORDS = [
    ("保修",   ["保修", "质保", "缺陷责任", "防水", "管线"]),
    ("付款",   ["付款", "支付", "结算", "进度款", "预付款", "工程款", "发票", "拨款"]),
    ("验收",   ["验收", "竣工", "交付", "移交", "接收", "组织验收"]),
    ("违约",   ["违约", "罚金", "罚款", "逾期", "赔偿", "责任"]),
    ("担保",   ["担保", "保证金", "保函", "履约", "投标"]),
    ("质保金", ["质保金", "质量保证金", "保修金"]),
    ("安全",   ["安全", "事故", "防护", "危险"]),
    ("争议",   ["争议", "仲裁", "诉讼", "管辖"]),
    ("工期",   ["工期", "开工", "竣工日期", "合同工期"]),
    ("工程概况", ["工程概况", "工程名称", "工程地点", "承包范围"]),
]

def _infer_clause_type(clause: ClauseBlock) -> tuple[str, float]:
    text = clause.clause_title + " " + clause.content[:200]
    for type_name, keywords in _CLAUSE_TYPE_KEYWORDS:
        if any(kw in text for kw in keywords):
            return type_name, 0.9
    return "其他", 0.0
```

**注意**：`_apply_classifier()`（第 333-353 行）只在 `split()` 传入了 classifier 参数时才覆盖 `_infer_clause_type` 的结果。T5.4 不传 classifier——_infer_clause_type 就是唯一类型来源。

### 第二步：验证

**先验证 clause_splitter 输出**（不需要跑全量评测，30 秒）：

```powershell
cd D:\shanmiao-kernel\shanmiao-kernel
python -c "
from app.engine.clause_splitter import ClauseSplitter
# 用云煤一矿合同测试
text = open('tests/contract_samples/yunmei_yikuang.txt', encoding='utf-8').read()
blocks = ClauseSplitter.split(text)
for b in blocks:
    print(f'{b.clause_id:10s} type={b.clause_type:6s} conf={b.type_confidence:.0%}  {b.clause_title[:50]}')
"
```

**确认**：不再出现 `"数字密集-xxx"` 或全 `"其他"`。付款条款标 `"付款"`、验收标 `"验收"`、保修标 `"保修"`。

**然后跑全量**：

```powershell
$env:DEEPSEEK_API_KEY="YOUR_API_KEY"
python tests/road2_compare.py
python -m pytest tests/ --ignore=tests/test_mus.py -q
```

验收标准：
- cn-008-FP-01 → 不再 false_positive（28天结算在付款 block，不在验收 block）
- cn-010-POS-01 → 不再机械加总增值税（税率在别的 block，不在付款 block）
- cn-008-POS-01 不退化（"竣工验收后28天"仍在验收 block）
- pytest 76/76
- extracted accuracy ≥ 93.48%（期望 95%+）

### 第三步：更新 known_limits.md

如果 cn-008-FP-01 和 cn-010-POS-01 修复生效，从 `tests/road2_known_limits.md` 移除这两条。

## 关键风险

**黄金样本都是短文本（≤100字）**——clause_splitter 可能只切出一个 block，关键词分类器对短文本的准确性需要验证。止损条件：如果 clause_type 标签错误率 > 20%，放弃 clause_type 方案，改用 context_pattern 否定锚点（在 handler 正则中排除结算关键词）。

**cn-008 退化风险**：改完后先跑 cn-008 三条样本单条验证。

## 改动范围

| 文件 | 改动 |
|------|------|
| `app/engine/clause_splitter.py` | `_infer_clause_type` 空壳→关键词表，~25行 |
| `tests/road2_known_limits.md` | 移除已修复的 cn-008/cn-010（若验证通过） |

**不改**：
- 不动 rules.json — clause_type 字段已在 29 条规则上就位
- 不动 kernel.py — 无需注入 classifier
- 不动 core.py（宪法 2.1）
- 不动 handler 代码（宪法 1.1）
- 不动 LLM 提取链路
- 不新建任何文件

## 关键文件

- 要改：`app/engine/clause_splitter.py:166-172`（`_infer_clause_type` 函数）
- 已就位（只读）：`domains/validated/construction/rules.json`（29 条规则已有 clause_type）
- 已就位（只读）：`app/engine/core.py:644-675`（clause_type scoping 逻辑）
- 已就位（只读）：`app/engine/handlers/numeric_comparison.py:83-97`（handler 侧 scoping）
- 评测：`tests/road2_compare.py` + `tests/road2_eval.py`
- 不可修文档：`tests/road2_known_limits.md`

