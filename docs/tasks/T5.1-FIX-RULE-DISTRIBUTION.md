# 任务单：T5.1 — 修复规则包分发缺口

> 发往新线程。自包含，无需上下文。

## 工作区

`D:\shanmiao-kernel\shanmiao-kernel\`

## 背景

T5 黄金数据集基线跑出 7 条不可达规则。真实根因已定位：

### 缺口位置

`app/kernel.py:268` — `_available_package_files()`：

```python
declared = manifest.files.get("rules_packages", [])
if declared:
    for fname in declared:
        if (domain_dir / fname).exists():
            available.append(fname)
    if available:
        return available  # ← 这里直接 return，不会加载 rules.json
```

`domains/validated/construction/domain.json:65` — 同时声明了：

```json
"rules_package": "rules.json",
"rules_packages": [
  "general-contract.json",
  "main-contract.json"
]
```

### 实际分布

| 文件 | 覆盖 numeric_comparison |
|------|------------------------|
| rules.json | 15/15 全有 |
| general-contract.json | 2/15（cn-019, cn-027） |
| main-contract.json | 6/15（cn-002, cn-003, cn-007, cn-008, cn-020, cn-021） |

cn-001/cn-004/cn-005/cn-006/cn-009/cn-010/cn-026 只在 rules.json 里，但 rules.json 被 rules_packages 分支绕过去了。

## 修复方案

采用方案 A（最稳）：

**只加载 rules.json 作为 construction 域的唯一权威运行包。** general-contract.json 和 main-contract.json 是历史子集分割，内容都在 rules.json 中。domain.json 的 files 字段改为只声明 rules_package，移除 rules_packages 数组（或注释掉）。

### 具体改动

1. **`domains/validated/construction/domain.json`** — 将 `files` 中的 `rules_packages` 数组注释掉或删除，保留 `rules_package: "rules.json"`
2. **`app/kernel.py`** — `_available_package_files()` 增加 fallback：如果 rules_packages 为空或其内文件不存在，用 `rules_package` 字段兜底（当前 fallback 列表只在两者都无时触发，需确保 rules_package 独立路径总可用）

## 验收标准

1. `python tests/road2_eval.py` 跑完后，MISSING 数量为 0
2. 15 条 road2 numeric_comparison 规则全部在 validate evidence_chain 中可见
3. `python -m pytest tests/ --ignore=tests/test_mus.py -q` 全量通过
4. 记录 rules.json/general/main 的主从关系说明，确保以后不再出现同一类规则散落在多个包的情况

## 约束

- 不动 core.py（宪法 1.1）
- 不动 handler（宪法 1.1）
- construction 域的其他 validated 域（foundation/nda/purchase）不受影响
- 改动范围仅限 `app/kernel.py` 和 `domains/validated/construction/domain.json`
